model User {
  id           String    @id @default(cuid())
  name         String
  email        String    @unique
  // phone        String    @unique
  passwordHash String
  bookings     Booking[]
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}